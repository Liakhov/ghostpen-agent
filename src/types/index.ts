export interface PostMeta {
  platform: string;
  topic: string;
  profile: string;
  created: string;
  iterations: number;
}

export type { StyleProfile, Voice, Platform, Example } from "./style-profile.js";
