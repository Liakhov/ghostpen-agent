# Style Profile Specification — Ghostpen v0.1

## Overview

Style Profile — єдине джерело правди про стиль користувача. Один JSON файл, який агент читає перед кожною генерацією і оновлює після підтвердженого фідбеку.

Профіль не намагається описати "ідеальний стиль". Він описує **як саме пише ця конкретна людина** — з усіма особливостями, перевагами і навіть недоліками, які роблять текст живим.

Розташування: `data/profiles/<profile-name>.json`

Ghostpen підтримує кілька профілів: один персональний (ваш стиль) і необмежену кількість reference-профілів (стилі інших авторів для аналізу та навчання).

---

## Multi-Profile System

### Типи профілів

| Тип | Опис | Еволюціонує з фідбеком | Використання |
|---|---|---|---|
| `personal` | Ваш стиль. Один на користувача. Створюється через `ghostpen init`. | Так | За замовчуванням для всіх генерацій |
| `reference` | Стиль іншого автора. Створюється через `ghostpen profile create`. | Ні (тільки вручну) | Генерація в чужому стилі, аналіз, mix-режим |

### Структура файлів

```
data/profiles/
├── default.json              ← персональний профіль (personal)
├── competitor-alex.json      ← reference профіль
├── competitor-maria.json     ← reference профіль
└── mentor-style.json         ← reference профіль
```

### CLI команди

```bash
# Персональний профіль
ghostpen init                                    # Створити default.json

# Reference профілі
ghostpen profile create competitor-alex          # Створити новий reference профіль
ghostpen profile list                            # Список всіх профілів
ghostpen profile show competitor-alex            # Показати summary профілю
ghostpen profile delete competitor-alex          # Видалити профіль

# Генерація з різними профілями
ghostpen "тема" --profile competitor-alex        # Писати у стилі Alex
ghostpen "тема" --mix default competitor-alex    # Мікс стилів (див. Mix Mode)
```

### Mix Mode

`--mix` приймає два профілі і створює гібрид за правилами:

- **З першого профілю (base):** tone, formality, personality, avoid, emoji_usage, signature_phrases
- **З другого профілю (techniques):** hooks, closings, sentence_style, paragraph_style, structure

Тобто: **"мій голос + його прийоми"**.

Агент отримує обидва профілі в контексті з чіткою інструкцією що звідки брати. Не механічне злиття JSON, а інтерпретація Claude на основі обох профілів.

Приклад system prompt для mix mode:
```
Ти генеруєш пост використовуючи два стилі:

BASE STYLE (голос, тон, характер): [Profile A]
TECHNIQUES (hooks, структура, прийоми): [Profile B]

Результат має звучати як автор A, але використовувати технічні прийоми автора B.
```

---

## Schema

```json
{
  "profile_name": "default",
  "profile_type": "personal",
  "source": "12 LinkedIn posts, 5 Instagram posts",
  "version": 1,
  "created_at": "2026-02-08T14:00:00Z",
  "updated_at": "2026-02-08T16:30:00Z",
  "language": "uk",

  "voice": {
    "tone": "",
    "formality": "",
    "personality": "",
    "sentence_style": "",
    "paragraph_style": "",
    "hooks": [],
    "closings": [],
    "signature_phrases": [],
    "avoid": [],
    "emoji_usage": ""
  },

  "platforms": {
    "linkedin": {},
    "instagram": {},
    "x": {}
  },

  "examples": [],

  "changelog": []
}
```

---

## Field Specification

### Root Fields

| Field | Type | Required | Description |
|---|---|---|---|
| `profile_name` | string | yes | Унікальне ім'я профілю (slug): `"default"`, `"competitor-alex"` |
| `profile_type` | string | yes | `"personal"` або `"reference"` |
| `source` | string | yes | Звідки взяті тексти для аналізу: `"12 LinkedIn posts, manually provided"` |
| `version` | number | yes | Інкрементується при кожному оновленні профілю |
| `created_at` | ISO 8601 | yes | Дата створення профілю |
| `updated_at` | ISO 8601 | yes | Дата останнього оновлення |
| `language` | string | yes | Основна мова контенту: `"uk"`, `"en"`, `"uk+en"` |

### Voice — ядро стилю

Секція `voice` описує як людина пише незалежно від платформи. Це "базовий шар" стилю, який платформи потім модифікують.

#### `voice.tone`
**Type:** string
**Description:** Загальний тон текстів в 2-4 словах.
**Examples:**
- `"дружній, трохи іронічний"`
- `"впевнений, прямий, без води"`
- `"теплий, розмовний, з гумором"`

Не використовувати generic слова типу "professional" чи "engaging" — вони нічого не значать. Тон має бути специфічним для цієї людини.

#### `voice.formality`
**Type:** string
**Description:** Рівень формальності на шкалі з поясненням.
**Examples:**
- `"casual але експертний — як розмова з колегою за кавою"`
- `"напівформальний — жодного сленгу, але і не корпоративний"`
- `"максимально неформальний — пише як у месенджері"`

#### `voice.personality`
**Type:** string
**Description:** Характер автора, який проступає в текстах. 2-3 речення.
**Examples:**
- `"Скептик з почуттям гумору. Любить ставити під сумнів загальноприйняте. Не боїться сказати 'я не знаю'."`
- `"Практик. Завжди дає actionable поради. Не терпить абстрактних порад без прикладів."`

#### `voice.sentence_style`
**Type:** string
**Description:** Як виглядають типові речення.
**Examples:**
- `"Короткі, рубані. Рідко більше 15 слів. Часто — фрагменти без дієслова."`
- `"Середньої довжини, з підрядними реченнями. Ритм: довге — коротке — довге."`
- `"Мікс. Довге пояснювальне речення, потім різке коротке для акценту."`

#### `voice.paragraph_style`
**Type:** string
**Description:** Як автор будує абзаци і візуальний ритм тексту.
**Examples:**
- `"Один абзац = одна думка. 2-3 речення максимум. Багато повітря між абзацами."`
- `"Щільні абзаци по 4-5 речень. Без зайвих розривів."`

#### `voice.hooks`
**Type:** string[]
**Description:** Типові прийоми для початку поста. Впорядковані від найчастішого до найрідшого.
**Examples:**
```json
[
  "провокативне твердження, яке хочеться оспорити",
  "особиста історія з першої особи",
  "питання до читача",
  "статистика або факт"
]
```

#### `voice.closings`
**Type:** string[]
**Description:** Типові прийоми завершення поста.
**Examples:**
```json
[
  "питання до аудиторії для дискусії",
  "короткий підсумок в одне речення",
  "call-to-action без тиску"
]
```

#### `voice.signature_phrases`
**Type:** string[]
**Description:** Фрази або конструкції, які автор використовує регулярно. Не кліше, а характерні мовні звички.
**Examples:**
```json
[
  "ось у чому штука",
  "давайте чесно",
  "і це нормально",
  "спойлер:"
]
```

#### `voice.avoid`
**Type:** string[]
**Description:** Що автор ніколи не пише. Табу. Антипатерни.
**Examples:**
```json
[
  "канцеляризми ('в рамках', 'з метою')",
  "слово 'контент' — каже 'пости' або 'тексти'",
  "мотиваційні кліше ('just do it', 'виходь із зони комфорту')",
  "пасивний стан ('було зроблено' замість 'я зробив')",
  "надмірні emoji",
  "хештеги в тілі тексту"
]
```

#### `voice.emoji_usage`
**Type:** string
**Description:** Як саме автор використовує emoji.
**Examples:**
- `"ніколи"`
- `"рідко, максимум 1-2 на пост, тільки для акценту"`
- `"часто, як частину ритму тексту, але не більше 5 на пост"`

---

### Platforms — платформо-специфічні правила

Кожна платформа може перевизначати або доповнювати базовий `voice`. Якщо поле не вказане — використовується значення з `voice`.

```json
{
  "platforms": {
    "linkedin": {
      "max_length": 1500,
      "structure": "hook (1-2 речення) → розвиток думки → особистий приклад → інсайт → питання до аудиторії",
      "tone_override": null,
      "formatting": "кожне речення з нового рядка, без списків",
      "hashtags": "3-5 хештегів в кінці, через рядок від основного тексту",
      "notes": "LinkedIn аудиторія цінує конкретику і досвід. Менше теорії, більше 'я спробував і ось що вийшло'."
    },
    "instagram": {
      "max_length": 800,
      "structure": "hook → цінність/порада → CTA",
      "tone_override": "трохи тепліший і ближчий ніж у LinkedIn",
      "formatting": "абзаци по 2-3 речення, emoji як роздільники секцій",
      "hashtags": "10-15 хештегів у першому коментарі, не в пості",
      "notes": "Instagram — більш емоційний. Менше логіки, більше історій."
    },
    "x": {
      "max_length": 280,
      "structure": "одна думка, максимально загострена",
      "tone_override": "різкіший, провокативніший",
      "formatting": "без форматування",
      "hashtags": "максимум 1, тільки якщо реально релевантний",
      "notes": "X — це спарринг. Кожен твіт має провокувати реакцію."
    }
  }
}
```

#### Platform Fields

| Field | Type | Required | Description |
|---|---|---|---|
| `max_length` | number | yes | Максимальна кількість символів |
| `structure` | string | yes | Типова структура поста для цієї платформи |
| `tone_override` | string \| null | no | Як тон відрізняється від базового `voice.tone`. null = без змін |
| `formatting` | string | yes | Правила форматування (абзаци, списки, розриви рядків) |
| `hashtags` | string | no | Правила використання хештегів |
| `notes` | string | no | Додаткові нотатки для агента про платформу |

---

### Examples — референсні пости

Масив з 3-5 найкращих постів користувача. Агент використовує їх як еталон для порівняння генерованого тексту.

```json
{
  "examples": [
    {
      "id": "ex_001",
      "platform": "linkedin",
      "text": "Повний текст поста...",
      "why_good": "Сильний hook через провокативне питання. Конкретний приклад з цифрами. Закінчується відкритим питанням.",
      "added_at": "2026-02-08T14:00:00Z"
    },
    {
      "id": "ex_002",
      "platform": "instagram",
      "text": "Повний текст поста...",
      "why_good": "Емоційна історія з першої особи. Чіткий takeaway. Природний CTA.",
      "added_at": "2026-02-08T14:00:00Z"
    }
  ]
}
```

#### Example Fields

| Field | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Унікальний ID (ex_001, ex_002...) |
| `platform` | string | yes | Для якої платформи цей приклад |
| `text` | string | yes | Повний текст поста |
| `why_good` | string | yes | Чому саме цей пост обраний як еталон. Агент генерує це поле при створенні профілю |
| `added_at` | ISO 8601 | yes | Коли додано |

---

### Changelog — історія змін

Кожне оновлення профілю записується. Це дозволяє відкатитись і зрозуміти еволюцію стилю.

```json
{
  "changelog": [
    {
      "version": 1,
      "date": "2026-02-08T14:00:00Z",
      "action": "created",
      "description": "Початковий профіль на основі 12 LinkedIn постів"
    },
    {
      "version": 2,
      "date": "2026-02-08T16:30:00Z",
      "action": "updated",
      "description": "Зменшено формальність: 'напівформальний' → 'casual але експертний'. Причина: 4 рази фідбек 'занадто формально'.",
      "fields_changed": ["voice.formality"]
    },
    {
      "version": 3,
      "date": "2026-02-09T10:00:00Z",
      "action": "updated",
      "description": "Додано 'спойлер:' до signature_phrases. Користувач вказав вручну.",
      "fields_changed": ["voice.signature_phrases"]
    }
  ]
}
```

#### Changelog Entry Fields

| Field | Type | Required | Description |
|---|---|---|---|
| `version` | number | yes | Номер версії після цієї зміни |
| `date` | ISO 8601 | yes | Коли змінено |
| `action` | string | yes | `"created"` або `"updated"` |
| `description` | string | yes | Що змінилось і чому, людською мовою |
| `fields_changed` | string[] | for updates | Які поля змінились (dot notation) |

---

## Full Example

Повний приклад заповненого профілю для українського LinkedIn-автора:

```json
{
  "profile_name": "default",
  "profile_type": "personal",
  "source": "12 LinkedIn posts, 5 Instagram posts",
  "version": 3,
  "created_at": "2026-02-08T14:00:00Z",
  "updated_at": "2026-02-09T10:00:00Z",
  "language": "uk",

  "voice": {
    "tone": "дружній, трохи іронічний, з повагою до читача",
    "formality": "casual але експертний — як розмова з розумним колегою за кавою",
    "personality": "Практик з досвідом. Любить розбирати загальноприйняті істини і показувати що все складніше. Не претендує на істину в останній інстанції. Часто ділиться власними помилками.",
    "sentence_style": "Короткі, рубані. Рідко більше 12 слів. Часто фрагменти: 'От і все.' або 'Ніяк.' Іноді довше речення для контрасту.",
    "paragraph_style": "Одна думка = один абзац. 1-3 речення. Багато повітря. Текст 'дихає'.",
    "hooks": [
      "провокативне твердження ('Контент-плани не працюють.')",
      "особиста історія ('Три роки тому я втратив всіх клієнтів за тиждень.')",
      "питання ('Коли востаннє ви чесно сказали клієнту «ні»?')",
      "неочікувана статистика"
    ],
    "closings": [
      "відкрите питання до аудиторії",
      "один рядок підсумку — різкий і запам'ятовуваний",
      "м'який CTA ('Якщо зайшло — збережіть')"
    ],
    "signature_phrases": [
      "ось у чому штука",
      "давайте чесно",
      "спойлер:",
      "і це нормально",
      "коротко:"
    ],
    "avoid": [
      "канцеляризми ('в рамках', 'з метою', 'дана стаття')",
      "слово 'контент' — каже 'пости' або 'тексти'",
      "мотиваційні кліше ('вийди із зони комфорту', 'прокачай')",
      "пасивний стан ('було прийнято рішення')",
      "більше 3 emoji на пост",
      "хештеги всередині тексту",
      "звертання 'друзі' до аудиторії"
    ],
    "emoji_usage": "рідко, 1-2 на пост, тільки ↓ → 🔥 для візуального акценту, ніколи як заміна слів"
  },

  "platforms": {
    "linkedin": {
      "max_length": 1500,
      "structure": "hook (1-2 рубаних речення) → контекст/історія → розвиток думки з прикладом → інсайт або висновок → питання до аудиторії",
      "tone_override": null,
      "formatting": "кожне речення або коротка думка з нового рядка. без маркованих списків. текст має виглядати як 'стіна з повітрям', а не як стаття.",
      "hashtags": "3-5 в кінці через порожній рядок",
      "notes": "LinkedIn аудиторія — професіонали. Цінують досвід і конкретику. 'Я спробував X і ось що вийшло' завжди працює краще ніж 'Топ-5 порад'."
    },
    "instagram": {
      "max_length": 800,
      "structure": "hook → 2-3 блоки цінності → CTA",
      "tone_override": "теплішій, ближчий, більше емоцій",
      "formatting": "абзаци по 2-3 речення. ↓ або → як роздільники секцій",
      "hashtags": "12-15 у першому коментарі",
      "notes": "Більше сторітелінгу, менше експертності. Люди скролять — перше речення вирішує все."
    },
    "x": {
      "max_length": 280,
      "structure": "одна загострена думка",
      "tone_override": "різкіший, без пом'якшень",
      "formatting": "без форматування, без хештегів в тілі",
      "hashtags": "0-1",
      "notes": "Твіт = одна думка доведена до краю. Якщо можна скоротити — скороти."
    }
  },

  "examples": [
    {
      "id": "ex_001",
      "platform": "linkedin",
      "text": "Контент-плани не працюють.\n\nЯ це зрозумів після того, як три місяці чітко слідував плану.\nІ отримав нуль реакцій.\n\nА потім одного ранку написав пост за 10 хвилин.\nПро те, що мене бісить в нашій індустрії.\nБез плану. Без структури.\n\n200+ коментарів.\n\nОсь у чому штука: люди реагують не на 'якісний контент'.\nВони реагують на чесність.\n\nПлан дає тобі теми.\nАле не дає тобі голос.\n\nА голос — це єдине, що відрізняє тебе від ще одного 'експерта' у стрічці.\n\nЯк у вас: плануєте чи пишете по натхненню?",
      "why_good": "Провокативний hook в одне речення. Особиста історія з конкретним результатом. Контраст (план vs спонтанність). Signature phrase 'ось у чому штука'. Закінчується питанням. Кожен рядок — окрема думка.",
      "added_at": "2026-02-08T14:00:00Z"
    },
    {
      "id": "ex_002",
      "platform": "instagram",
      "text": "Три роки тому я втратив всіх клієнтів за один тиждень.\n\nНе через кризу. Не через конкурентів.\nЧерез одне рішення, яке здавалось правильним.\n\n↓\n\nЯ підняв ціни вдвічі.\nБез попередження. Без пояснення.\nПросто виставив нові рахунки.\n\nРезультат: 6 з 6 клієнтів пішли.\n\n↓\n\nАле через 2 місяці у мене було 4 нових.\nЯкі платили нову ціну без питань.\n\nДавайте чесно: я тоді не був розумний.\nМені просто пощастило.\n\nАле урок залишився:\nправильна ціна відлякує неправильних клієнтів.\nІ це нормально.",
      "why_good": "Інтригуючий hook. Емоційна історія з поворотом. Чесне визнання помилки. Дві signature phrases ('давайте чесно', 'і це нормально'). Стрілки як роздільники. Компактні абзаци.",
      "added_at": "2026-02-08T14:00:00Z"
    }
  ],

  "changelog": [
    {
      "version": 1,
      "date": "2026-02-08T14:00:00Z",
      "action": "created",
      "description": "Початковий профіль на основі 12 LinkedIn та 5 Instagram постів"
    },
    {
      "version": 2,
      "date": "2026-02-08T16:30:00Z",
      "action": "updated",
      "description": "Зменшено формальність. Причина: 4 рази фідбек 'занадто формально'.",
      "fields_changed": ["voice.formality"]
    },
    {
      "version": 3,
      "date": "2026-02-09T10:00:00Z",
      "action": "updated",
      "description": "Додано 'спойлер:' до signature_phrases. Додано правило про звертання 'друзі' в avoid.",
      "fields_changed": ["voice.signature_phrases", "voice.avoid"]
    }
  ]
}
```

---

## How the Agent Uses This

### Генерація з одним профілем

1. Агент визначає який профіль використовувати (`--profile` або `default`)
2. Читає повний профіль
3. Визначає платформу з запиту
4. Бере `voice` як базу + `platforms[platform]` як модифікатор
5. Перевіряє `examples` для цієї платформи як еталон тону
6. Генерує, свідомо уникаючи все з `avoid`
7. Перевіряє `max_length` перед видачею

### Генерація в mix mode

1. Агент читає обидва профілі
2. З першого (base) бере: tone, formality, personality, avoid, emoji_usage, signature_phrases
3. З другого (techniques) бере: hooks, closings, sentence_style, paragraph_style, platform structure
4. Формує комбінований контекст для Claude
5. Генерує пост, де голос — твій, а прийоми — запозичені
6. В metadata згенерованого файлу зберігає обидва профілі

### При оновленні

1. Агент отримує фідбек
2. Перевіряє `profile_type`:
    - `personal` → може оновити автоматично після підтвердження
    - `reference` → пропонує оновити тільки якщо користувач додає нові пости цього автора
3. Класифікує фідбек: конкретна правка чи стильова
4. Якщо стильова і `feedback_counter >= 3` → пропонує зміну
5. Після підтвердження: оновлює поле, інкрементує `version`, додає `changelog` запис
6. Зберігає файл

### Валідація

Агент перевіряє при читанні:
- `version` — number, > 0
- `language` — один з `["uk", "en", "uk+en"]`
- `voice.hooks` — не порожній масив
- `voice.avoid` — не порожній масив
- `platforms` — хоча б одна платформа
- `examples` — хоча б один приклад

Якщо валідація failed → показати що саме не так, запропонувати виправити.